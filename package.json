{
  "name": "adnotia",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "license": "AGPL-3.0-only",
  "engines": {
    "node": ">=24"
  },
  "description": "Free, open-source, evidence-based tools for adults with ADHD and for parents of children who may have it. Everything runs in the browser.",
  "scripts": {
    "dev": "vite",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:parity": "vitest run tests/parity --passWithNoTests",
    "typecheck": "tsc --noEmit",
    "build": "vite build",
    "build:single": "vite build --mode single && node scripts/finish-single.mjs",
    "check": "npm run typecheck && node scripts/check-no-network.mjs"
  },
  "devDependencies": {
    "@types/node": "^24.13.3",
    "jsdom": "^30.0.1",
    "typescript": "^7.0.2",
    "vite": "^8.2.2",
    "vite-plugin-pwa": "^1.3.0",
    "vite-plugin-singlefile": "^2.3.3",
    "vitest": "^5.0.0"
  }
}
